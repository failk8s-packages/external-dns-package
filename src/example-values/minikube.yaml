#! Add your values here (without ytt annotations)
#! domain: example.com
infraProvider: "custom"
aws:
  credentials:
    accessKey: XXX
    secretKey: YYY
deployment:
  args:
    - --provider=aws
    - --source=service
