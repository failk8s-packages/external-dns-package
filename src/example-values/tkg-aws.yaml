#! Add your values here (without ytt annotations)
domain: example.com
provider: aws
target:
  ingress: True
  contour: True
aws:
  auth:
    iam:
      access_key_id: XXX
      secret_access_key: YYY
